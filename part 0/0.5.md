sequenceDiagram
participant browser
participant server
activate browser
Note right of browser:Note created
Note right of browser:Append to the existing notes
Note right of browser:Calls redraw function to renser new note to the page
deactivate browser
browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/spa
Note right of browser:The user's new note content and timestamp is sent as a json object as the payload to the server
activate server
  server->> browser:201 response
  Note right of browser:server responds with JSON{"message":"notes created"}to denote the note was successfully created
deactivate server  